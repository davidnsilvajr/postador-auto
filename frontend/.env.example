# Frontend - variaveis de ambiente (Vite)
# Copie para .env e ajuste.

# URL do backend. Em dev pode deixar vazio (o Vite faz proxy de /api para :8000).
# Em producao, aponte para a URL do backend, ex: https://api.seudominio.com
VITE_API_URL=

# user_id de dev (uuid de um usuario existente na tabela `profiles` do Supabase).
# Usado enquanto nao ha login completo. Pegue em Supabase > Authentication > Users.
VITE_DEV_USER_ID=
